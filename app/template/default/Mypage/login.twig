{% extends 'default_frame.twig' %}
{% set body_class = 'mypage' %}

{% block main %}
    <div class="ec-role">
        <div class="ec-pageHeader">
            <h1>{{ 'common.login'|trans }}</h1>
        </div>
        <div class="ec-off2Grid">
            <div class="ec-off2Grid__cell">
                <form name="login_mypage" id="login_mypage" method="post" action="{{ url('mypage_login') }}">
                    {% if app.session.flashBag.has('eccube.login.target.path') %}
                        {% for targetPath in app.session.flashBag.peek('eccube.login.target.path') %}
                            <input type="hidden" name="_target_path" value="{{ targetPath }}" />
                        {% endfor %}
                    {% endif %}
                    <div class="ec-login">
                        <div class="ec-login__icon">
                            <div class="ec-icon">
                                <img src="{{ asset('assets/icon/user.svg') }}" alt="" />
                            </div>
                        </div>
                        <p>会員登録をされている方は、メールアドレス・パスワードを入力しログインしてください。</p>
                        <div class="ec-login__input">
                            <div class="ec-input">
                                <label>メールアドレス</label>
                                {{ form_widget(form.login_email, {'attr': {'style': 'ime-mode: disabled;', 'autofocus': true} }) }}
                                <label>パスワード</label>
                                {{ form_widget(form.login_pass) }}
                            </div>
                            {% for reset_complete in app.session.flashbag.get('password_reset_complete') %}
                                <p>{{ reset_complete|trans }}</p>
                            {% endfor %}
                            {% if error %}
                                <p class="ec-errorMessage">{{ error.messageKey|trans(error.messageData, 'validators')|nl2br }}</p>
                            {% endif %}
                            <div class="ec-login__link">
                                <a class="ec-link" href="{{ url('forgot') }}">パスワードをお忘れの方</a>
                            </div>
                            {% if BaseInfo.option_remember_me %}
                                <div class="ec-checkbox">
                                    <label>
                                        {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                                            <input type="hidden" name="login_memory" value="1" />
                                        {% else %}
                                            {{ form_widget(form.login_memory, {'label': 'common.remember_me'|trans}) }}
                                        {% endif %}
                                    </label>
                                </div>
                            {% endif %}
                        </div>
                        <div class="ec-grid2">
                            <div class="ec-grid2__cell">
                                <div class="ec-login__actions">
                                    <button type="submit" class="ec-blockBtn--cancel">ログインしてトップページへ</button>
                                </div>
                            </div>
                        </div>
                    </div>
                    <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}" />
                </form>
                <div class="form_bottom">
                    <p class="b_head">紋別市ふるさと納税総合サイトが<br>はじめての方</p>
                    <div class="ec-grid2__cell">
                        <p class="mb-4">
                            当サイトでふるさと納税のお申込みするには2つの方法ございます。<br>
                            会員登録をしてお申込みする方法と、会員登録をせず、その都度お申込みをする方法です。<br><br>
                            会員登録(無料)をしていただくと、ご利用時、情報入力の手間が省けます。また、マイページ機能を使って、寄付履歴の確認など、より簡単に・便利に寄付を行うことが出来ますので会員登録をしてお申込みする方法をおすすめいたします。
                        </p>
                        <div class="ec-login__link">
                            <a class="ec-link" href="{{ url('entry') }}">新規会員登録（無料）</a>
                        </div>
                        <div class="b_secure">
                            <img src="{{ asset('assets/img/secure.png', 'user_data') }}" alt="" />
                            <p>お申込者の入力情報は、全て暗号化されて守られています。</p>
                        </div>
                        <p>
                            <i style="color:#ffcc00;" class="fa fa-exclamation-triangle fa-fw"></i>
                            当サイトを会員登録をされてご利用になる場合は、（旧）紋別市ふるさと納税特設サイトで会員登録されていた方も新たにご登録ください。（旧）特設サイトでご登録された会員情報は引き継がれておりませんので予めご了承ください。<br><br>
                            また、ふるさとチョイス・楽天ふるさと納税・さとふる・ふるなびなどの各ふるさと納税ポータルサイトで紋別市にふるさと納税のお申込をされた方も当サイトを初めてご利用になる場合は、新規会員登録が必要となりますので予めご了承ください。
                        </p>
                        <a href="javascript:history.back()" class="to-back">前の画面に戻る</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
